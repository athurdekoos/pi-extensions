{
  "name": "adk-metadata-schema",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Canonical schema contract for .pi-adk-metadata.json — shared by pi-google-adk and pi-subagents",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}
